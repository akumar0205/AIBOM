# AIBOM
Automatic AIBOM Generator

## Usage

Run the generator against a Python project directory:

```bash
python aibom_generator.py /path/to/project -o AI_BOM.json
```

If no target is provided, the current directory is scanned recursively for `.py` files.
