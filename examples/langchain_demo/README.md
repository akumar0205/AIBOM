# LangChain Demo

Run your LangChain app, then generate AIBOM and evidence:

```bash
aibom generate . --audit-mode --bundle-out evidence.zip
```

This repository's analyzer captures runtime-relevant imports/calls through static signatures compatible with callback-based LangChain instrumentation patterns.
